Work through the following three steps in order, in this one session. Do not
skip ahead, and do not answer until all three are done.

Step 1. Every file matching the glob `*_test.go` begins with a header comment of
the exact form `// CHECKSUM: <integer>`. Find all of them, read each CHECKSUM
value, and compute their sum. Call this value A.

Step 2. Exactly one file under each of `src/core/`, `src/util/`, and `src/api/`
defines a function named `ProcessBatch` that takes no arguments and returns a
literal int. Open each of those three files and read the returned integer.
Compute the sum of the three. Call this value B.

Step 3. Compute A multiplied by B.

Report exactly three integers separated by a comma and a single space, in the
order A, B, then the product — nothing else, no explanation, no units.

EXPECTED: ^171, 99, 16929$
