Read the file `src/core/invoice.go`. It defines exactly one function that
computes an invoice total. Report only that function's exact identifier, as
written in the source (case-sensitive) — nothing else, no punctuation, no
explanation.

EXPECTED: ComputeInvoiceTotal
