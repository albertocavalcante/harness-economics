Exactly one file under each of `src/core/`, `src/util/`, and `src/api/`
defines a function named `ProcessBatch` that takes no arguments and returns
a literal int. Find that function in each of the three module directories,
and report the sum of the three returned integers as a single integer —
nothing else, no punctuation, no explanation.

EXPECTED: 99
