This repository contains a number of files matching the glob `*_test.go`.
Each one starts with a header comment of the exact form
`// CHECKSUM: <integer>`. Find every file matching that pattern, read its
CHECKSUM value, and report the sum of all of them as a single integer —
nothing else, no punctuation, no explanation.

EXPECTED: ^171$
